<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
	  <Version Condition=" '$(TRAVIS_BUILD_NUMBER)' == '' ">0.0.1-local</Version>
    <Version Condition=" '$(TRAVIS_BUILD_NUMBER)' != '' ">1.0.$(TRAVIS_BUILD_NUMBER)</Version>
    <Authors>Sanatana</Authors>
	  <RepositoryUrl>https://github.com/RodionKulin/Sanatana.MongoDb</RepositoryUrl>
	  <PackageLicenseUrl>https://github.com/RodionKulin/Sanatana.MongoDb/blob/master/LICENSE</PackageLicenseUrl>
	  <PackageDescription>MongoDb repository and extensions</PackageDescription>
	  <PackageTags>MongoDb;Repository;Extensions;</PackageTags>
    <PackageReleaseNotes>Repository added</PackageReleaseNotes>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
    <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'">
    <DocumentationFile>D:\dev\Собственное\Библиотеки\Sanatana.MongoDb\Sanatana.MongoDb\Sanatana.MongoDb.xml</DocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="MongoDB.Driver" Version="2.10.1" />
  </ItemGroup>

</Project>
